## 1. 工具與設定檔

- [x] 1.1 將 `josephspurrier/goversioninfo/cmd/goversioninfo` 透過 `go get -tool github.com/josephspurrier/goversioninfo/cmd/goversioninfo` 加入 `go.mod` 的 `tool ( ... )` 區段，使 `go tool goversioninfo` 可用且版本鎖於 `go.sum`，符合既有 `go tool lefthook` / `go tool golangci-lint` 的工具管理慣例（CLAUDE.md 規範）。Verified by `grep goversioninfo go.mod` 找到該模組於 `tool ( ... )` 區段內、以及 `go tool goversioninfo -help` 退出碼 0。
- [x] 1.2 建立 `versioninfo.json` 於 repo 根目錄，作為 Resource embedding is Windows-amd64 only and source-controlled requirement 的 single source-of-truth。內容包含 (a) `ManifestOptions` 區塊指向最小化 manifest（`requestedExecutionLevel level="asInvoker" uiAccess="false"`、無 compatibility/dpiAware）符合 Application manifest suppresses Installer Detection requirement；(b) `FixedFileInfo.FileVersion` / `FixedFileInfo.ProductVersion` 設為 `0.0.0.0` placeholder；(c) `StringFileInfo` 含 `ProductName=rancher-kubeconfig-updater`、`FileDescription=Update kubeconfig tokens for Rancher-managed Kubernetes clusters`、`CompanyName` = repo owner、`LegalCopyright=MIT License - see LICENSE`，符合 Windows version info populated from release tag requirement。Verified by (a) 以 `python3 -m json.tool versioninfo.json` 解析無誤；(b) `go tool goversioninfo -platform-specific versioninfo.json` 在 repo 根目錄執行成功並產出 `resource_windows_amd64.syso`。
- [x] 1.3 在 `.gitignore` 加入 `*.syso` 規則，符合 Resource embedding is Windows-amd64 only and source-controlled requirement 中 generated artefact MUST NOT be committed 條款。Verified by 跑 `go tool goversioninfo -platform-specific versioninfo.json` 產出 `resource_windows_amd64.syso` 後，`git status --short` 不顯示該檔，且 `git check-ignore -v resource_windows_amd64.syso` 列印出對應的 `.gitignore` 規則行。

## 2. Build 流程整合

- [x] 2.1 在 `main.go` 加入 `//go:generate go tool goversioninfo -platform-specific versioninfo.json` directive（無 build tag、跨平台都能執行），實作 Resource generation invoked from build entry points requirement 的 local 路徑。Verified by (a) 在 macOS / Linux 跑 `go generate ./...` 成功產出 `resource_windows_amd64.syso`（即使 host 不是 Windows）；(b) `grep "go:generate" main.go` 命中該 directive；(c) 後續 `go build .`（非 Windows host）成功且 binary 大小與 pre-change byte-identical。
- [x] 2.2 修改 `Makefile` 的 `build` target，在 `go build .` 之前插入 `go generate ./...`，落實 Resource generation invoked from build entry points requirement 中 Local make build embeds placeholder version scenario。Verified by 在 macOS 跑 `make build`，斷言 (a) `resource_windows_amd64.syso` 被產出但因檔名 platform-suffix 而被 Go linker 忽略；(b) Linux/macOS 產出 binary byte-identical 於不含此 change 的 commit；(c) 重複 `make build` 兩次第二次仍成功（generate 為 idempotent）。
- [x] 2.3 修改 `.github/workflows/release-please.yml` 的 `build-and-upload` job：(a) 從 `${{ needs.release-please.outputs.tag_name }}` 解析 `vX.Y.Z` 為三個整數環境變數 VER_MAJOR/VER_MINOR/VER_PATCH（用 `sed` 或 bash parameter expansion）；(b) **僅當** `matrix.goos == 'windows'` 時，於 `go build` 之前加一步 `go tool goversioninfo -platform-specific -ver-major=$VER_MAJOR -ver-minor=$VER_MINOR -ver-patch=$VER_PATCH versioninfo.json`；(c) Linux/macOS 列不加此步驟。落實 Resource generation invoked from build entry points requirement 的兩個 scenarios（CI build injects release tag、Non-Windows CI rows skip generation）。Verified by (a) 以 `act` 或 push 到 fork repo 觸發 workflow，斷言 Windows job 的 log 含 `goversioninfo` 呼叫且 `-ver-major` / `-ver-minor` / `-ver-patch` 反映 release tag；(b) Linux/darwin job 的 log 不含 `goversioninfo` 字串；(c) workflow 退出碼 0。

## 3. 文件

- [x] 3.1 在 `README.md` 的 `Building from Source` 段落補一句：Windows 從原始碼建置時需先跑 `go generate ./...`（或直接 `make build`）才能嵌入 manifest，否則產出的 binary 仍會觸發 UAC。Verified by 閱讀更新後的 `README.md`，確認 Building from Source 區塊出現該說明且不影響 Linux/macOS 步驟。

## 4. 端到端驗證

- [x] 4.1 在 `windows-amd64` 主機驗證 Launching the binary from a standard user shell does not prompt for UAC scenario：以非 admin PS session 跑 `.\rancher-kubeconfig-updater.exe --help`，斷言 (a) 無 UAC prompt；(b) 輸出 usage 文字於當前 console（不另開視窗）；(c) 退出碼 0。Verified by 手動執行並截錄輸出。
- [x] 4.2 在同一主機驗證 Released binary shows the release tag in File Properties scenario：對由 CI 產出的對應 release tag binary，於 Windows Explorer 右鍵 → 內容 → 詳細資料，比對 ProductVersion / FileVersion / ProductName / FileDescription 欄位值符合 spec 的 release tag mapping。Verified by 截圖或 PowerShell `(Get-Item .\rancher-kubeconfig-updater.exe).VersionInfo | Format-List` 輸出。
- [x] 4.3 驗證 Linux and macOS artefacts are unaffected scenario：在乾淨 worktree 上抓取 pre-change main 與 post-change main 的 `goos=linux goarch=amd64` build 產出 binary，以 `sha256sum` 比對應該 byte-identical（兩個 commit 的其他差異僅來自 versioninfo.json / main.go go:generate directive / .gitignore / Makefile / workflow，皆不影響 Linux build output）。Verified by 手動比對 hash。
