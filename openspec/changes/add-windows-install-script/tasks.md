## 1. 實作 install.ps1

- [x] 1.1 建立 `install.ps1`（PowerShell 5.1+ 相容，開頭強制 TLS 1.2、ASCII-only 訊息、`exit 0/1`），構成 Windows 版本的 One-line installation entry point。Verified by 在 `pwsh` 與 `powershell.exe` 內以 `Set-StrictMode -Version Latest` 載入腳本（不執行 main flow），確認語法解析無錯誤；以 `PSScriptAnalyzer` 對 `install.ps1` 跑預設規則集無 Error/Warning 級別發現。
- [x] 1.2 實作 Platform allowlist with fail-fast behavior 的 Windows 分支：讀取 `$env:PROCESSOR_ARCHITECTURE`（並在 WOW64 下 fall back 至 `$env:PROCESSOR_ARCHITEW6432`），normalize 為 `amd64` / `arm64`；allowlist 僅 `windows-amd64`，其他組合（特別是 `windows-arm64`）在任何網路呼叫之前 fail-fast，stderr 含 `windows-arm64` 字串與 Building from Source 指引。Verified by 透過內建測試 hook（例如 `$env:_ARCH_OVERRIDE='ARM64'`，與 install.sh 的 `_OS_OVERRIDE` 命名一致）模擬 ARM64 主機，斷言 exit code 非零、stderr 含 `windows-arm64`、且未產生網路請求（用 `Get-Counter` 或乾脆斷網檢測）。
- [x] 1.3 實作 Version selection via VERSION environment variable 的 Windows 分支：未設或 `latest` 時走 `releases/latest/download/rancher-kubeconfig-updater-windows-amd64.exe`，否則走 `releases/download/${VERSION}/...`；HTTP 4xx/5xx 退出非零、stderr 含失敗 URL、目標路徑無 partial file。display_version 透過 `Invoke-WebRequest -Method Head -MaximumRedirection 0` 取得 `Location` header 並用 regex 抽 release tag，失敗則 fallback 為 `latest` 字串。Verified by 設定 `$env:VERSION='v0.0.0-nonexistent'` 後執行腳本，斷言 exit code 非零、stderr 含完整下載 URL 與 HTTP 404 字樣、target directory 無新增 `.exe` 檔。
- [x] 1.4 實作 Installation directory via INSTALL_DIR environment variable 的 Windows 分支：預設 `$env:USERPROFILE\.local\bin`、未設或為空時加 `USERPROFILE` 守衛；預設目錄不存在自動以 `New-Item -ItemType Directory -Force` 建立、建立失敗退出非零；非預設值不存在 fail-fast、不 mkdir、不 UAC；非預設值存在但不可寫 → 退出非零並指引用 admin PS 重跑（**不**用 `Start-Process -Verb RunAs` 提權）。Verified by (a) `$env:USERPROFILE=$(mktemp dir-fake)` 後執行 → 目錄被建立、`.exe` 落地；(b) `$env:INSTALL_DIR='C:\does-not-exist'` → exit 非零、stderr 含該路徑、無 `New-Item` 呼叫（用 `Get-EventLog` 或 transcripts 確認）；(c) `$env:INSTALL_DIR='C:\Program Files\rkc'` 模擬不可寫（以 non-admin 跑）→ exit 非零、stderr 指引 admin PS、無 `Start-Process -Verb RunAs` 呼叫。
- [x] 1.5 實作 Windows 預設目錄的 User PATH 自動寫入：用 `[Environment]::GetEnvironmentVariable('PATH', 'User')` 讀取現行使用者 PATH，若 `INSTALL_DIR` 不在其中則 `SetEnvironmentVariable('PATH', "$existing;$INSTALL_DIR", 'User')` 寫入，並在 confirmation line 後印明確 notice 提示需重啟 shell 才生效；當前 session 的 `$env:PATH` **不**修改；非預設 `INSTALL_DIR` **不**改 PATH。Verified by (a) 預設值首次安裝（PATH 內無該目錄）→ 安裝後 `[Environment]::GetEnvironmentVariable('PATH', 'User')` 含該目錄、stderr 含 restart-shell notice；(b) 預設值二次安裝（PATH 內已含）→ User PATH 字串與安裝前 byte 一致、無 notice；(c) `$env:INSTALL_DIR='C:\tools'`（user-writable 非預設）→ User PATH 未被修改。
- [x] 1.6 下載走 `New-TemporaryFile`（或 `[System.IO.Path]::GetTempFileName()`）staging + `Move-Item -Force` 到目標路徑，確保下載失敗或被中斷時不留 partial `.exe`。`try/finally` 清掉 staging 檔案。Verified by (a) `$env:VERSION='v0.0.0-nonexistent'` 後執行 → 觸發 404、`$env:USERPROFILE\.local\bin\rancher-kubeconfig-updater.exe` 不存在；(b) 模擬下載中斷（kill curl / 拔網路）→ `.local\bin` 內無 `.tmp` 殘留檔案。

## 2. 更新 README

- [x] 2.1 改寫 `Installation > Windows` 子段落使 README installation section uses the script 在 Windows 也精準：呈現單行 `irm https://raw.githubusercontent.com/chenwei791129/rancher-kubeconfig-updater/main/install.ps1 | iex`、列出 `$env:VERSION` / `$env:INSTALL_DIR` 變數用法（PowerShell 語法）、明示支援平台僅 `windows-amd64`、`linux-arm64` / `darwin-amd64` / `windows-arm64` 等其他平台指向 Building from Source。Verified by 閱讀更新後的 `README.md`，確認 Windows 子段落首行為 `irm ... | iex`、含環境變數表格、且舊有 4 步驟 PowerShell snippet 已完全移除。
- [x] 2.2 補上 Windows system-wide 安裝範例：使用 admin PowerShell session、顯式設定 `$env:INSTALL_DIR='C:\Program Files\rancher-kubeconfig-updater'`，並提示該路徑需事先 `New-Item -ItemType Directory -Force` 建立（因 spec 規定非預設值不自動 mkdir）。Verified by 閱讀 README，確認 system-wide 範例命令出現 `Start a new elevated PowerShell` 或同義指示、且 `$env:INSTALL_DIR` 範例值含 `Program Files` 字樣。
- [x] 2.3 擴充 `Building from Source` 子段落涵蓋 windows-arm64 等未發佈 Windows 平台：在現行 `git clone` + `go build .` 段落補充 PowerShell 等價指令（或維持單一指令並注明跨平台適用）。Verified by 閱讀 README 該段落，確認任一未發佈平台的使用者依步驟能產出可運作的 binary（在 windows-arm64 上以 `go build -o rancher-kubeconfig-updater.exe .` 預期成功）。

## 3. 端到端驗證

- [ ] 3.1 在 Windows 10/11 `windows-amd64` 主機以乾淨環境（先 `Remove-Item -Recurse -Force "$env:USERPROFILE\.local\bin"`）執行 `irm https://raw.githubusercontent.com/.../install.ps1 | iex`，驗證 One-line installation entry point 的 Default install on Windows scenario：目錄被建立、binary 落地、User PATH 被追加、新開 shell 能直接執行 `rancher-kubeconfig-updater --help` 並退出碼 0、舊 shell 提示需重啟。Verified by 手動執行並截錄輸出 + 開新 PowerShell session 跑 `--help`，比對 spec 中該 scenario 的 THEN 條款。
- [ ] 3.2 在同一主機驗證 Non-default directory requires admin elevation (Windows) scenario：non-admin PowerShell session + `$env:INSTALL_DIR='C:\Program Files\rkc-test'`（先以 admin 預建空目錄、確認 non-admin 不可寫），執行腳本後斷言 exit 非零、stderr 含「elevated」字樣與該路徑、User PATH 未變、目標路徑無 `.exe`。Verified by 手動執行並比對 spec scenario 的 THEN 條款。
