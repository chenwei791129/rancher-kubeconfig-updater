# Rancher Configuration
RANCHER_URL=https://rancher.example.com
RANCHER_USERNAME=your-username
RANCHER_PASSWORD=your-password

# Auth type, defaults to "local", can be "ldap" or "local"
RANCHER_AUTH_TYPE=local

# Skip TLS certificate verification (insecure, use only for development/testing)
# Uncomment and set to "true" to disable TLS verification
# RANCHER_INSECURE_SKIP_TLS_VERIFY=false

# Token expiration threshold in days (default: 30)
# Tokens expiring within this threshold will be regenerated
# TOKEN_THRESHOLD_DAYS=30

# Force refresh all tokens regardless of expiration (default: false)
# FORCE_REFRESH=false
